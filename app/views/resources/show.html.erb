<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @resource.title %>
</p>

<p>
  <strong>Summary:</strong>
  <%= @resource.summary %>
</p>

<p>
  <strong>Link:</strong>
  <%= @resource.link %>
</p>
<br>

 <%#= render :partial => @resource.comments %>


<h3>Comments</h3>
   <% for comment in @resource.comments %>
   <br>
      <h5>
      <%= comment.body %>
      </h5>
      <p>
      <%= "Comment posted by: #{@resource.user.name}" %>
      </p>
      <br>
      
<% end %>
<br><br>

<h3>Post a Comment</h3>

<%= form_for([@resource, @resource.comments.build]) do |f| %>
 
  <div class="field">
    <% f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <p><%= f.submit %></p>
<% end %>

<%= link_to 'Edit', edit_resource_path(@resource) %> |
<%= link_to 'View all resources', resources_path %>
