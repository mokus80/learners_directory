<%= form_for([@resource, @resource.comments.build]) do |f| %>

<%= render 'layouts/error_messages' %>
 
  <div class="field">
    <% f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <p><%= f.submit %></p>
<% end %>
